@{
    # TEMPLATE — copie como eav-config.psd1 no share \\SEU-SERVIDOR\EAV$ e
    # preencha com os valores reais (que ficam no cofre da TI, nao aqui).
    # O share deve ser restrito a Domain Computers.

    Token            = 'COLE_O_AGENT_SYNC_TOKEN_AQUI'
    ServerIP         = 'tech.escolaamericana.com.br'
    RustDeskPassword = 'COLE_A_SENHA_RUSTDESK_AQUI'
}
